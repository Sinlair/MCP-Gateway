spring:
  application:
    name: mcp-gateway

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

managed-systems:
  big-market:
    name: big-market-71772-z
    repo-path: "E:\\Internship\\program\\big-market-71772-z"
    base-url: "http://127.0.0.1:8091"
    api-version: v1
